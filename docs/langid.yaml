openapi: "3.0.0"
info:
  title: Language Identification — Lift Wing inference service
  version: v1
servers:
  - url: https://api.wikimedia.org
paths:
  /service/lw/inference/v1/models/langid:predict:
    post:
      summary: Identify the language of a text
      description: |
        Identifies the language of a given text using a fastText language
        identification model. For more information, see the [model card](https://meta.wikimedia.org/wiki/Special:MyLanguage/Machine_learning_models/Production/Language_Identification).
      operationId: langidPredict
      tags:
        - Countries and language
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [text]
              properties:
                text:
                  type: string
                  description: Text to identify the language of. Newlines and tabs are normalised. Truncated to 100 characters.
                  example: "A wiki is a form of hypertext publication on the internet which is collaboratively edited and managed by its audience directly through a web browser."
            example:
              text: "A wiki is a form of hypertext publication on the internet which is collaboratively edited and managed by its audience directly through a web browser."
      responses:
        "200":
          description: Successful prediction
          content:
            application/json:
              schema:
                type: object
                properties:
                  language:
                    type: string
                    description: Detected language code
                    example: "eng_Latn"
                  wikicode:
                    type: string
                    description: Wikimedia language code
                    example: "en"
                  languagename:
                    type: string
                    description: Full language name in English
                    example: "English"
                  score:
                    type: number
                    format: float
                    description: Confidence score
                    example: 0.392
                    minimum: 0.0
                    maximum: 1.0
              example:
                language: "eng_Latn"
                wikicode: "en"
                languagename: "English"
                score: 0.392
        "400":
          description: Invalid input
