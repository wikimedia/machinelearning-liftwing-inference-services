# Service
fastapi==0.136.1
uvicorn==0.46.0
pydantic>=2.5

# Fetch + parsing
requests==2.32.5
lxml>=5.0

# Text normalization (heavy; optional in dev, required in the prod image.
# Without it clean_spoken_text falls back to the regex path, and
# generation_version reflects that via the "regex" engine tag.)
nemo_text_processing==1.2.0

# ASCII transliteration for the Latin characters espeak cannot pronounce
# (ISC licensed, no dependencies). Its table feeds clean_spoken_text, so
# its output is part of content_sha256: pin it, and move the version
# component in version.py with any upgrade (T438647).
anyascii==0.3.3

# Object storage (s3 sink; credentials via AWS_ACCESS_KEY_ID/SECRET env)
boto3==1.43.54
