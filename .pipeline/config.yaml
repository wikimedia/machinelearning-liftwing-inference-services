pipelines:
  outlink:
    blubberfile: outlink/blubber.yaml
    stages:
      - name: run-test
        build: test
        run: true
      - name: production
        build: production

  outlink-publish:
    blubberfile: outlink/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-outlink'
            tags: [stable]

  revertrisk:
    blubberfile: revertrisk/revertrisk.yaml
    stages:
      - name: run-test
        build: test
        run: true
      - name: production
        build: production

  revertrisk-publish:
    blubberfile: revertrisk/revertrisk.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-revertrisk'
            tags: [stable]

  revertrisk-multilingual-publish:
    blubberfile: revertrisk/multilingual.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-revertrisk-multilingual'
            tags: [stable]

  revertrisk-wikidata-publish:
    blubberfile: revertrisk_wikidata/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-revertrisk-wikidata'
            tags: [stable]

  revscoring-publish:
    blubberfile: revscoring/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-revscoring'
            tags: [stable]

  ores-legacy-publish:
    blubberfile: ores-legacy/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-ores-legacy'
            tags: [stable]

  llm-publish:
    blubberfile: llm/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-llm'
            tags: [stable]

  readability-publish:
    blubberfile: readability/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-readability'
            tags: [stable]

  langid-publish:
    blubberfile: langid/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-langid'
            tags: [stable]

  article-descriptions-publish:
    blubberfile: article_descriptions/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-article-descriptions'
            tags: [stable]

  huggingface-publish:
    blubberfile: huggingface/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-huggingface'
            tags: [stable]

  logo-detection-publish:
    blubberfile: logo_detection/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-logo-detection'
            tags: [stable]

  articlequality-publish:
    blubberfile: articlequality/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-articlequality'
            tags: [stable]

  reference-quality-publish:
    blubberfile: reference_quality/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-reference-quality'
            tags: [stable]

  article-country-publish:
    blubberfile: article_country/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-article-country'
            tags: [stable]

  editing-suggestions:
    blubberfile: editing_suggestions/blubber.yaml
    stages:
      - name: run-test
        build: test
        run: true
      - name: production
        build: production

  editing-suggestions-publish:
    blubberfile: editing_suggestions/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-editing-suggestions'
            tags: [stable]

  edit-check:
    blubberfile: edit_check/blubber.yaml
    stages:
      - name: run-test
        build: test
        run: true
      - name: production
        build: production

  edit-check-publish:
    blubberfile: edit_check/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-edit-check'
            tags: [stable]

  pre-commit-check:
    blubberfile: pre-commit-check/blubber.yaml
    stages:
      - name: run-test
        build: test
        run: true

  revise-tone-task-generator:
    blubberfile: revise_tone_task_generator/blubber.yaml
    stages:
      - name: run-test
        build: test
        run: true
      - name: production
        build: production

  revise-tone-task-generator-publish:
    blubberfile: revise_tone_task_generator/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-revise-tone-task-generator'
            tags: [stable]

  embeddings-publish:
    blubberfile: embeddings/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-embeddings'
            tags: [stable]

  policy-violation-gpt-oss-safeguard-publish:
    blubberfile: policy_violation/blubber_gpt_oss_safeguard_20b.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-policy-violation-gpt-oss-safeguard'
            tags: [stable]

  qwen36:
    blubberfile: qwen36/blubber.yaml
    stages:
      - name: run-test
        build: test
        run: true

  qwen36-publish:
    blubberfile: qwen36/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-qwen36'
            tags: [stable]

  policy-violation-cope-a-9b-publish:
    blubberfile: policy_violation/blubber_cope_a_9b.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-policy-violation-cope-a-9b'
            tags: [stable]

  policy-violation-cope-b-a4b-publish:
    blubberfile: policy_violation/blubber_cope_b_a4b.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-policy-violation-cope-b-a4b'
            tags: [stable]

  liftwing-openapi-server:
    blubberfile: liftwing_openapi_server/blubber.yaml
    stages:
      - name: production
        build: production

  liftwing-openapi-server-publish:
    blubberfile: liftwing_openapi_server/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-liftwing-openapi-server'
            tags: [stable]

  tts-publish:
    blubberfile: tts/blubber.yaml
    stages:
      - name: publish
        build: production
        publish:
          image:
            name: '${setup.project}-tts'
            tags: [stable]
